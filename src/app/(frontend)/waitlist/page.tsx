import LandingFrame from '@/components/LandingFrame'

export const metadata = {
  title: 'Join the waitlist · Zali',
  description:
    'Zali turns your emails, messages and tools into tasks — and actually helps you finish them. Join the waitlist.',
}

export default function WaitlistPage() {
  return <LandingFrame section="waitlist" />
}
